import { FilesExeptionFilter } from './files-exeption.filter';

describe('FilesExeptionFilter', () => {
  it('should be defined', () => {
    expect(new FilesExeptionFilter()).toBeDefined();
  });
});
