import { BodyInterceptorTsInterceptor } from './body.interceptor.ts.interceptor';

describe('BodyInterceptorTsInterceptor', () => {
  it('should be defined', () => {
    expect(new BodyInterceptorTsInterceptor()).toBeDefined();
  });
});
